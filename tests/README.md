# Tests

Tests cover raw schema failures, deterministic selection, panel integrity, development-only EDA/segments, exact feature timing, perturbation invariance, item isolation, row-order determinism, metric formulas, seasonal-naive identity, fixed model features/categories, weekly training cutoffs, validation-only model choice, model-specification timing, prediction clipping, shared metrics, and exact feature-importance coverage. Milestone 5 will add dashboard tests.
